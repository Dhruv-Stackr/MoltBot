# Nixpacks configuration for Railway
# Using npm instead of yarn for better compatibility

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]  
cmds = ["npm run build"]

[start]
cmd = "npx serve -s build -p $PORT"
