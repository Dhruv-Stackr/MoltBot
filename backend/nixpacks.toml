# Add latest Node.js to the Python environment
[phases.setup]
nixPkgs = ['nodejs']

# Install clawdbot after pip installs requirements.txt (Nixpacks auto-handles this)
[phases.postInstall]
cmds = ['bash install_railway.sh']

[start]
cmd = 'uvicorn server:app --host 0.0.0.0 --port $PORT'
