# Nixpacks configuration for Railway  
# Using npm (comes with Node.js by default)

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve -s build -p $PORT"
