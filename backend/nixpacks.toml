[phases.setup]
nixPkgs = ['python312', 'nodejs_22', 'curl']

[phases.install]
cmds = [
    'pip install -r requirements.txt',
    'bash install_moltbot_deps.sh'
]

[phases.build]
cmds = ['echo "Build phase complete"']

[start]
cmd = 'uvicorn server:app --host 0.0.0.0 --port $PORT'
