# Nixpacks configuration for Railway
# This tells Railway to use Node.js 22 with yarn pre-installed

[phases.setup]
nixPkgs = ["nodejs_22", "yarn-berry"]

[phases.install]
cmds = ["yarn install --frozen-lockfile"]

[phases.build]
cmds = ["yarn build"]

[start]
cmd = "npx serve -s build -p $PORT"
